name: Release

on:
  push:
    branches: [ release ]
  workflow_dispatch:

jobs:

  release:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
    - uses: actions/checkout@v2

    - name: Set up Elm
      uses: justgook/setup-elm#1.2.0

    - run: npm -v
